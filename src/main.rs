// constだとグローバルスコープ設定できる。letはエラー
const B: i32 = 2;

fn main() {
    // // 変更できないいミュータブル変数
    // let a = 1;
    // println!("{}", a);

    // // a = 2;
    // // ミュータブル変数
    // let mut b = 2;
    // b = 3;

    // // 型を指定して、変数の初期化を別々にすることができるが、初期化を忘れるとエラーになる
    // let c: i32;
    // c = 43;

    // // 変数宣言の変更：シャドーイング
    // let c: &str = "test";

    //========定数===========
    const A: i32 = 1;
    println!("{}", A);
    println!("{}", B);
}

